# Copy to observability/.env and adjust if needed.
# Security is disabled for localhost workshop use only.

ES_VERSION=8.15.3
ES_PORT=9200
KIBANA_PORT=5601

# Used by detection-tools shippers and mock-server hooks
SCAS_ES_URL=http://localhost:9200
