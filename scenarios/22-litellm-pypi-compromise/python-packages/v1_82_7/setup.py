from setuptools import find_packages, setup

setup(
    name="litellm_like",
    version="1.82.7",
    description="Testbench stand-in: import-time telemetry (simulated compromised release)",
    packages=find_packages(),
    python_requires=">=3.8",
)
