# secure-validator

Legitimate **v2.5.3** reference implementation for the Scenario 3 lab (input validation and sanitization).

Compare with `../../compromised-package/secure-validator` (v2.5.4 simulation) to review what a malicious patch might add.
