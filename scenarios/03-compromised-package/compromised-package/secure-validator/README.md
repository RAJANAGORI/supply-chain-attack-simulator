# secure-validator (simulated compromise)

**v2.5.4** lab build: same public API as v2.5.3 with **intentional malicious behavior** for this testbench only (see `index.js` and `../../templates/compromised-package-template.js`).

**Do not publish or reuse outside this course.**
